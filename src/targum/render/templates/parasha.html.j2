{#- This week's Torah portion, with the reading itself inside it.

    The weekly's anatomy, and deliberately so: a pitch, then the product working on the
    thing the visitor came for. What differs is what the two are for. The news weekly
    catches somebody learning to read a newspaper; this catches somebody who is going to
    hear this portion read on Shabbat and would like to follow it.

    The corpus behind it is fixed — the same fifty-four, forever — so nothing on this
    page is generated week to week. A calendar says which portion, and the page points
    at a reader that was built once. -#}
<!doctype html>
<html lang="en">
<head>
{% include '_public_head.html.j2' %}
<style>{{ asset('reader.css') }}{{ asset('public.css') }}</style>
{{ hebrew_face(true) }}
<script>{{ asset('theme.js') }}</script>
</head>
<body class="public">
<main class="sheet weekly parasha">
  <div class="masthead">
    <a class="brand" href="/" aria-label="targum">
      <svg class="brand-mark" viewBox="20 12 56 72" aria-hidden="true" focusable="false">
        <rect x="20" y="12" width="22" height="62" rx="5" fill="currentColor"></rect>
        <rect x="54" y="22" width="22" height="62" rx="5" fill="var(--mark-accent)"></rect>
      </svg>
      <span>targum</span>
    </a>
    <div class="corner">
      <a class="enter" href="/account/signin">Sign in</a>
      <button type="button" data-theme-toggle aria-label="Switch theme"></button>
    </div>
  </div>

  {#- The hero: a band the width of the window, split down the middle.

      The picture is a photograph of a Torah scroll, which §10 used to forbid outright and
      as of 2026-09-01 allows on a content surface — see §12 for the rule and its
      conditions. Why the seam is vertical rather than a scrim over the picture: a
      headline set over a photograph *of writing* is a fight you win at best, by dimming
      the photograph until it is a texture. Turning the seam upright means no word is ever
      over the picture, so the type keeps full contrast and the picture keeps full
      strength. On a phone the seam turns with the layout.

      The words are the page's own h1 and its portion name, not a caption: what is on the
      ink half is the same thing that was on the paper hero before it. -#}
  <section class="band">
    <div class="said">
      {#- On the page that means "this Shabbat" the eyebrow says so; on a portion asked
          for by name it says where in the Torah it is, because that page is not about a
          week. Either way it is not the same words as the label further down. -#}
      <span class="eyebrow">{% if this_week %}This Shabbat{% else %}{{ portion.books | join(' · ') }}{% endif %}</span>
      {#- The headline names what the page is actually showing. On a portion asked for
          by name, "this week's parasha" was a lie — and fifty-two pages sharing one
          headline is also fifty-two pages a search engine cannot tell apart. -#}
      <h1 class="pitch">{% if this_week %}This week's parasha, every word explained.{% else %}{{ portion.name }}, every word explained.{% endif %}</h1>
      {% if portion.hebrew or portion.name %}
      <span class="named" lang="he" dir="rtl">{{ portion.hebrew or portion.name }}</span>
      {% endif %}
    </div>
    {#- Decoration: it says nothing the words have not said, so it is hidden from a
        screen reader rather than described to one. -#}
    <div class="pic" aria-hidden="true">
      <img src="{{ data_uri('scroll/columns.jpg') }}" alt="">
    </div>
  </section>

  <section class="hero">
    <p class="lede">
      The Hebrew as the Masorah wrote it, with the chanting marks or without, a
      translation beside every verse, and any word you tap remembered.
    </p>
    {#- The name is Hebrew inside an English sentence, so it is isolated wherever it
        appears: without that the bidi algorithm drags the surrounding punctuation to
        the wrong end and the button reads backwards. -#}
    <p class="cta">
      <a class="go" href="#embed">Read <bdi lang="he" dir="rtl">{{ portion.hebrew or portion.name }}</bdi></a>
    </p>
  </section>

  <h2 class="label">The reading</h2>
  {#- The dateline says which portion, when, and how much of it — the four things
      somebody preparing checks before they start. -#}
  <p class="dateline">
    <bdi class="named" lang="he" dir="rtl">{{ portion.hebrew or portion.name }}</bdi>
    is read on {{ shabbat_said }}{% if hdate %} ({{ hdate }}){% endif %} —
    {{ portion.summary }}, {{ portion.verses }} verses in
    {{ portion.aliyot }} aliyot.{% if portion.kind.value == 'festival' %}
    A festival falls on this Shabbat, so this is what is read in its place rather than
    the portion the cycle would otherwise have reached.{% elif portion.doubled %}
    Two portions are read together this week.{% endif %}
  </p>

  {#- The two ways to read it. Not levels — the text is the text — but the one choice
      that changes what is on the page: whether the chanting marks are on it. Links, so
      the page works with no JavaScript at all; `parasha.js` upgrades them to a switch
      that changes the reader in place. -#}
  <nav class="ladder taamim" aria-label="The chanting marks">
    <a href="?taamim=on" data-taamim="on"{% if taamim %} class="here" aria-current="page"{% endif %}>
      <b>With the te'amim</b>
      <span class="figure">as it is chanted</span>
      <span class="what">Every mark the Masorah wrote: the vowels, and the cantillation the reading is sung to.</span>
    </a>
    <a href="?taamim=off" data-taamim="off"{% if not taamim %} class="here" aria-current="page"{% endif %}>
      <b>Vowels only</b>
      <span class="figure">easier to read</span>
      <span class="what">The chanting marks taken off, the vowels left where they were — the plainer page for reading rather than leyning.</span>
    </a>
  </nav>

  {#- Israel and the diaspora read different portions for some weeks of the year, and
      for the rest they read the same one. Two things follow. It is drawn only on the
      page that means "this Shabbat" — on a named portion there is no week to disagree
      about, and the question is not being asked. And it names both portions only when
      they are actually two: a toggle offering the same name twice is a control that
      teaches nothing. -#}
  {% if this_week and diaspora and israel %}
  {% set apart = diaspora.slug != israel.slug %}
  <p class="schedules">
    {% if apart %}
    <span class="apart">Israel and the diaspora are reading different portions this week.</span>
    {% endif %}
    <a class="schedule{% if schedule.value == 'diaspora' %} here{% endif %}"{% if schedule.value == 'diaspora' %} aria-current="page"{% endif %} href="/parasha?schedule=diaspora">
      Diaspora{% if apart %} · <bdi lang="he" dir="rtl">{{ diaspora.hebrew }}</bdi>{% endif %}
    </a>
    <a class="schedule{% if schedule.value == 'israel' %} here{% endif %}"{% if schedule.value == 'israel' %} aria-current="page"{% endif %} href="/parasha?schedule=israel">
      Israel{% if apart %} · <bdi lang="he" dir="rtl">{{ israel.hebrew }}</bdi>{% endif %}
    </a>
  </p>
  {% endif %}

  <div class="embed" id="embed">
    <button type="button" class="handle" id="embed-handle" aria-expanded="false">Full screen</button>
    {#- The first aliyah, not the contents page. A portion is built in seven sections —
        one per aliyah, which is also one chanted file per page — and `index.html` is
        then a list of seven links, which is a poor first thing to hand somebody who came
        to read. The reader's own pager goes on from here, and the contents page is a
        press away inside it. -#}
    <iframe
      src="/parasha/read/{{ portion.folder }}/reader/sec-0001.html"
      title="{{ portion.name }}"
      loading="lazy"></iframe>
  </div>

  <section class="how">
    <h2 class="label">Why targum</h2>
    <p class="section-title">Built for reading, not for drills.</p>
    <ol>
      <li>
        <b>Every word, one tap.</b>
        <span>A card says what it means, how common it is and how it is said; tell it how well you know the word, and it remembers.</span>
      </li>
      <li>
        <b>The marks, on or off.</b>
        <span>The te'amim are what the reading is chanted to and clutter if you are still learning the vowels underneath them. One press takes them off and leaves everything else where it was.</span>
      </li>
      <li>
        <b>Seven aliyot, seven sections.</b>
        <span>The reading is divided the way it is called up, so you can go to שלישי without counting verses.</span>
      </li>
      <li>
        <b>It follows you.</b>
        <span>Words you mark here are marked in everything else you read — this week's portion, next week's, and the newspaper.</span>
      </li>
    </ol>
  </section>

  <section class="honest">
    <h2 class="label">Made honestly</h2>
    <p class="made">
      The Hebrew is the Masoretic text as it is published, cut to this week's reading and
      not edited. The English beside it is {{ translation_said }}. The division into
      aliyot and the calendar behind it — including which portion Israel reads and which
      the diaspora reads — come from Hebcal.
    </p>
    {#- Whose work this page is built out of. The photograph asks for nothing — it is
        CC0 — and is credited anyway, which is the rule the recordings already follow:
        a credit that lives only in a repository is a credit nobody reading ever sees.
        The recording's licence does require it. -#}
    <p class="made credits">
      The scroll at the top is
      <a href="https://commons.wikimedia.org/wiki/File:Closeup_of_Torah_Scroll.jpg"
         rel="noopener noreferrer">photographed by Jacob Gucker</a>, in the public domain
      under CC0. Where a reading has a recording it is
      <a href="https://archive.org/details/PockettorahAudioFiles"
         rel="noopener noreferrer">PocketTorah</a>, chanted in the Avery-Binder trope and
      used under <a href="https://creativecommons.org/licenses/by-sa/3.0/"
         rel="noopener noreferrer">CC BY-SA 3.0</a>.
    </p>

    {% if listed %}
    {#- Every portion, all year, because the corpus is fixed and there is no reason to
        make somebody wait a year to read one. Folded on a phone. -#}
    <details class="sources" id="sources" open>
      <summary><b class="sub">Every portion <span class="count">({{ listed|length }})</span></b></summary>
      <ul class="portions">
        {%- for one in listed %}
        <li>
          <a class="story" href="/parasha/{{ one.slug }}" lang="he" dir="rtl">{{ one.hebrew or one.name }}</a>
          <span class="row">
            <span class="outlets" lang="en" dir="ltr">{{ one.name }}</span>
            <span class="licence">{{ one.summary }}</span>
          </span>
        </li>
        {%- endfor %}
      </ul>
    </details>
    {% endif %}
  </section>

  <section class="keep">
    <h2 class="label">Keep what you learn</h2>
    <p class="section-title">Every word you mark, remembered.</p>
    <p class="pitch-line">In next week's portion, and in everything you read after it.</p>
    <p class="door">
      <a class="go" href="/account/signin">Sign in</a>
      <span class="free">An email address. No password.</span>
    </p>
  </section>
</main>

<script>{{ asset('parasha.js') }}</script>

{% include '_public_foot.html.j2' %}
</body>
</html>
